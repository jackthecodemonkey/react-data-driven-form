const rules = Object.freeze({
    minLength: 'minLength',
    maxLength: 'maxLength',
    pattern: 'pattern',
    required: 'required',
    minSelect: 'minSelect',
    maxSelect: 'maxSelect',
})

export default rules;
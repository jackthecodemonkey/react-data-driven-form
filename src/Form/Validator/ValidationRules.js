const rules = Object.freeze({
    minLength: 'minLength',
    maxLength: 'maxLength',
    regexp: 'regexp',
    required: 'required',
    optionSelected: 'optionSelected',
    minSelect: 'minSelect',
    maxSelect: 'maxSelect',
})

export default rules;